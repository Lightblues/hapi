# tencent-hapi
## 2026-03-04: `tencent-hapi` reverse
我通过如下形式安装了一个私有的包 (似乎没有开源代码?) 它改写的是 [github hapi](https://github.com/tiann/hapi)
```sh
npm config set registry https://mirrors.tencent.com/npm/
npm install -g @tencent/hapi@latest
```
帮我分析一下这个包
- 它是如何实现 对于 `claude-internal` 命令行的执行的 (因为非交互模式 `claude-internal -p "query"` 用的模型会变掉)
- 会有什么安全问题吗? 主要针对 claude-internal & claude-code

1. 能否基于开源版 hapi 自己实现一个针对 claude-internal 的代理呢? 这样是不是更可控一点, 也方便我分析
2. hapi 的日志都保存在 `~/.hapi` 目录下, 帮分析一下有什么有价值的信息

我测试了一下, 使用 `--output-format stream-json` 这种兴趣运行必然需要 `-p` 模式 (非交互), 此时 `claude-internal` 会自动切换到国内模型
1. 调研一下开源版 hapi 是如何实现对于 claude code 的支持的? 如何实现远程访问?
2. 讨论一下内网版 hapi 的实现, 会不会是基于那个hook? 或者基于对于 sessions/ 内容 .jsonl 文件的解析呢?

## 2026-03-05: analysis tencent-hapi
我拿到 `tencent-hapi` 的源码了! git@git.woa.com:ViteOps/tencent-hapi.git 这个可以 clone 下来.
我的后续计划:
- 在 github 开源版 hapi 的基础上进行开发, 仅仅引入内网版的 claude-internal 的远程访问功能. (避免冗余功能)
- 后续部署我自己的 relay 机器, 融入我的个人开发工作流.
你需要:
- 分析一下其针对 claude-internal 的中转机制 (local/remote 模式), 分析迁移难度
- 是否还有其他的相较于开源版的特色功能

claude-internal 和 claude 的一个核心差异是, 使用 -p (--print) 模式启动的时候, 内部不会采用 claude 模型, 而是会跳转为司内部署的deekseep模型!
`claude-internal -p --output-format stream-json --verbose`
- 我需要避免这一问题的发生!
- 你先帮我分析一下 tencent-hapi 底层是如何调用 claude-internal 的? 先去分析一下它是如何避免该问题的, 验证ok之后再去做迁移!

// 帮我验证一下, 用 --input-format stream-json 模式启动 claude-internal 确认模型不会切换
```sh
  ┌─────────────┬──────────────────────────────────────────────────────┬─────────────┬──────────────────────┐
  │    场景     │                       启动方式                       │    使用     │         模型         │
  │             │                                                      │  --print?   │                      │
  ├─────────────┼──────────────────────────────────────────────────────┼─────────────┼──────────────────────┤
  │ Local 模式  │ claude-internal 直接启动，stdio: inherit             │ 否          │ Claude（正常）       │
  ├─────────────┼──────────────────────────────────────────────────────┼─────────────┼──────────────────────┤
  │ Remote 模式 │ claude-internal --output-format stream-json          │ 否          │ Claude（正常）       │
  │             │ --input-format stream-json                           │             │                      │
  ├─────────────┼──────────────────────────────────────────────────────┼─────────────┼──────────────────────┤
  │ Metadata    │ claude-internal --output-format stream-json --print  │ 是          │ DeepSeek（但不影响） │
  │ 探测        │ "hello"                                              │             │                      │
  └─────────────┴──────────────────────────────────────────────────────┴─────────────┴──────────────────────┘
```


# hapi
// spec
整理上面的调研结果到 @.scribe/specs/cc_hapi/spec.md 和 @.scribe/specs/cc_hapi/progress.md 文件, 遵循规范英文spec写法

## 2026-03-05: impl
// 开始迁移吧，先把 directory-scan 和 stream-json 的核心逻辑移植到开源版 hapi
// plan & exec
// now build the exe and test it with `hapi claude-internal`
```sh
# build
cd ~/.ea/vesta/code/repos/tiann/hapi && bun install && bun run build
hapi claude-internal
```
// commit this

下面, 参考 @.scribe/specs/cc_hapi/spec.md 帮我分析一下应该如何验证 remote 场景的有效性? 我如何能够在网页上进行测试呢
```sh
bun src/index.ts hub
```
我运行 `bun src/index.ts hub` 启动 [hub](http://localhost:3006/) 之后, 页面上显示需要 access key
// 输入 ~/.hapi/setting.json 里面的key

## 2026-03-05: web add `claude-internal` & debug
我在 web 上测试成功了. 帮我分析一下后续的开发方向
- 在远程交互的入口增加专门的 claude-internal 的启动项 (类似 tencent-hapi 那样) -- 感觉这样可以优化体验, 我直接在web上选择使用 claude / claude-internal
- 配置远程 relay 服务 -- 这样我可以在远程进行操纵了! 这里需要需要如何实现呢?
- 使用 tencent-hapi 的时候, 我可以直接在web上开启一个新的 claude session -- 我猜是因为在机器上启动了一个后台进程? 在原版 hapi 中应该如何配置呢
// 

// web: 新增 `claude-internal` 入口
帮我给 hapi 的web增加 claude-internal 的入口 (确认是 /Users/frankshi/.ea/vesta/code/repos/tiann/hapi/ 我的定制版hapi)
// restart the hub and test it in the browser

1. token 采用 `auth` 命令的方式来登录, 不要手动配置key了
2. 我重新登录了 [web](http://localhost:3006/sessions/new) , 还是没有看到 claude-internal 的启动选项
    1. `Machine` 里面也没有看到机器

还是只能看到下面五个启动命令, 是因为没有build吗? 还是别的原因?
    claude
    codex
    cursor
    gemini
    opencode
还是说因为 @tencent/hapi 干扰了? 是不是因为 uninstall/stop 原本 `hapi` 命令和 Runner?
// 定位: Hub 从 web/dist/ 提供静态文件。需要 build web

现在有了, 不过测试下来有问题:
1. web hub 上, 新建一个不在 Recent paths 里的目录 (e.g. /Users/frankshi/.ea/vesta/code/repos/tiann/hapi) 会报错;
2. 复用之前的session, 发送消息之后, `Process exited unexpectedly`
我重新测试了 tencent-hapi, 没有这些问题. 帮我分析日志定位问题

后面正确的日志是因为我又测试了内网版的 `hapi` 命令 (通过 npm 安装的), 你帮我关掉它; 然后继续测试 /Users/frankshi/.ea/vesta/code/repos/tiann/hapi

测试出现的两个问题: 1) claude-internal 新建选项又没有了! 2) 在之前没有的目录 (e.g. /Users/frankshi/.ea/vesta/code/repos/tiann/hapi/) 还是显示报错 (Unexpected spawn result)
// spec
汇总当前的debug结论&开发状态到 progress 中
// 问题定位: Dev mode 下 runner spawn 因 bun 路径解析 bug 失败
// fix the dev-mode runner spawn issue

## 2026-03-05: `claude-internal` debug
@.scribe/specs/cc_hapi/progress.md
我们之前更新了 hapi 的 web 页面, 但是我测试下来发现在web端新创建一个session之后, 发现任意执行一个query, 都有问题: `Process exited unexpectedly`
e.g. session_id=264a2986-1656-4045-9d72-de3b10cd5df5
// 定位: claude-internal 不支持 --settings flag（这在progress.md 里已经记录），导致它立即以 exit code 1 退出，触发 Process exited unexpectedly


# relay
## 2026-03-05: hapi `relay` and `app`
我测试基本功能OK了! 接下来, 我希望能够实现远程访问 -- 类似 <https://app.hapi.run/> 的入口.
- 帮我分析一下, 1. 可以复用 hapi 的服务器吗; 2. 是否需要我部署一个服务/域名
- 相关: @.scribe/specs/cc_happy/spec.md 里面配置了一个 relay 服务器
    - 需要的话, 可以通过 `ssh tx01` 在服务器上部署
ref: @.scribe/specs/cc_hapi/packages.md
```sh
  本地 Mac:
    Hub (localhost:3006)
      ↕ tunwg (隧道客户端)
      ↕ WireGuard/TCP 加密通道
    relay.hapi.run (官方中继服务器)
      ↕
    app.hapi.run (官方 Web PWA，纯静态)
      → 通过 URL 参数连接到你的 Hub: app.hapi.run/?hub=<tunnel_url>&token=<token>
```

探讨一下:
1. app.hapi.run 
    1. 功能维度, 这个是提供了PWA的应用吗? 我理解官方的 hapi 的页面没法开 claude-internal
    2. 因此, 是不是需要我自己针对我 fork 的特有版本来部署?
2. relay.hapi.run
    1. 这个目标是实现 relay? 是否会存储数据呢? 不过不存储的话是不是用官方的也行? 
    2. 如果会存储一些重要信息的话, 我感觉需要私有部署
// 

1. 帮我配置 tunwg 并安装 `hapi` 命令 (注意把之前npm安装的 tencent-hapi 卸载 / 换名为 hapi-internal)
    1. 注意, 用官方推荐方式来安装 [tunwg](https://github.com/ntnj/tunwg)
```sh
go install github.com/ntnj/tunwg/tunwg@latest
```

// 问题: `hapi hub --relay` 跑不通
```sh
  ┌─────────────────────────┬────────────────┬──────────┬────────────┐
  │          组件           │      地址      │   直连   │ 通过 proxy │
  ├─────────────────────────┼────────────────┼──────────┼────────────┤
  │ l.tunwg.com (tunwg API) │ 107.173.82.160 │ TLS 超时 │ OK         │
  ├─────────────────────────┼────────────────┼──────────┼────────────┤
  │ relay.hapi.run (relay)  │ 43.130.231.139 │ TLS 超时 │ OK         │
  └─────────────────────────┴────────────────┴──────────┴────────────┘
```
// 问题是 tunwg 本身是个 WireGuard 隧道工具，它直接走 UDP/TCP 连 relay 服务器，不走 HTTP proxy，所以在国内直连失败。


陈清一下:
1. `hapi hub --relay` 的作用是什么? 为什么还会申请一个 [公网 url](https://xfgf6m43u432o4c4.relay.hapi.run) 出来?
    1. 它和 `hapi hub` 的差异是什么呢?
    2. 我尝试 `unsetproxy` 关闭终端代理, 还是跳 `[Tunnel] Waiting for trusted TLS certificate...` 之后没反应
// hapi hub（无 relay）: 只能在本机访问!
// hapi hub --relay: 不仅启动本机 hub server (`http://localhost:3006`); 
//  额外启动 tunwg 子进程，通过 WireGuard 隧道连接 relay.hapi.run
//  relay 分配一个公网域名（如 https://xfgf6m43u432o4c4.relay.hapi.run） -- 后续直接访问该地址就是本机的 hub!

- 先梳理一下 `hapi --help` 的这些命令, 它们分别有什么作用? 有哪些端口占用?
    - hapi run 启动的后台任务有什么用? 
    - 单运行命令 `hapi` 的时候会不会默认配置下面的服务?
```sh
  ┌─────────────────────────────────────────────────────┐
  │  hapi hub (:3006)                                    │
  │  - HTTP API + Web UI + SQLite                        │
  │  - SSE 推送给浏览器                                    │
  │  - Socket.IO 接收 CLI 上报                            │
  │  - RPC 下发指令给 Runner                              │
  └───────────────┬─────────────────────────────────────┘
                  │ RPC: "spawn claude-internal session"
  ┌───────────────▼─────────────────────────────────────┐
  │  hapi runner (后台守护进程, 随机端口)                    │
  │  - 接收 Hub 的 spawn 指令                             │
  │  - 执行 spawnHappyCLI() 创建 Claude 子进程            │
  │  - 管理已 spawn 的 session 生命周期                    │
  └───────────────┬─────────────────────────────────────┘
                  │ spawn
  ┌───────────────▼─────────────────────────────────────┐
  │  hapi claude[-internal] (每个 session 一个进程)        │
  │  - 包装 Claude Code CLI                              │
  │  - Socket.IO 连 Hub，上报消息事件                      │
  │  - local 模式: 直接终端交互                            │
  │  - remote 模式: stdin/stdout JSON stream              │
  └─────────────────────────────────────────────────────┘
```

## 2026-03-05: ssh tunnel & supervisor
// ssh tunnel
帮我配置 SSH 隧道方案, 通过 tx01 把 localhost:3006 暴露到公网
- 之前有个类似的 ssh tunnel 服务 @/Users/frankshi/Library/LaunchAgents/com.ea.tunnel.tx02.plist
// `https://hapi.easonsi.site` | /Users/frankshi/Library/LaunchAgents/com.ea.tunnel.hapi.plist

可以了! 讨论一些优化点
1. cliApiToken 目前的token是用 hapi-internal 的时候创建的, 复用它是否会有风险?
    1. 相关地, 我需要测试 hapi-internal 的话, 如何避免端口占用/配置文件冲突?
2. hapi.easonsi.site 的安全性问题: 目前是只要token一样就能放我我远程的 hapi runner/hub 吗?
3. 是不是可以把 `hapi hub` 配置到 @supervisord.conf 中, 长期后台运行?
// hapi.easonsi.site 安全性: 加密形式就是 **token-based auth**, 和 app.hapi.run 的安全模型一致

修复之前配置的 supervisor cc_hapi: 报错: `/opt/homebrew/bin/hapi: line 4: exec: bun: not found`
// 修复: 在 /opt/homebrew/bin/hapi 里面硬编码 bun 地址

// spec 
同步SPEC文件, 记录我们上轮的讨论&实现
- 重写 @.scribe/specs/cc_hapi/spec.md 去掉 Implementation 部分的细节, 从而保证 spec 文件提纲挈领, 强调技术架构&决策考量
    - 代码细节有必要的话另写一个文档?
- 完全重写 @.scribe/specs/cc_hapi/progress.md 这个文件记录的应该实时过程中经验教训 & 决策, 避免重犯错误


## sep `hapi-internal` and `hapi`
帮我处理 `hapi-internal` 和 `hapi` 命令隔离的问题. 我希望配置如下, 从而避免文件/端口冲突
    `HAPI_HOME=~/.hapi-internal HAPI_LISTEN_PORT=3007`
如何对 hapi-internal 命令默认用这种配置呢?
// 方案: 修改 /opt/homebrew/bin/hapi-internal 文件 (环境变量) -- `hapi-internal` 直接运行还是有问题, 删?


## 2026-03-25: debug `hapi`
帮我重新评估一下 @spec/cc_hapi/spec.md 我这个魔改版 `hapi` 项目, 尤其是它是如何处理 claude-internal CLI 的?

帮我配置启动一下, 我测试看看

修复一个bug: 本地项目中启动terminal是OK的, 但是在 hapi.easonsi.site 中启动会失败: 
`/socket.io/?EIO=4&transport=polling&t=o3h9n0a9&sid=QEmuLgxnZYQuXD1uy4qh:1  Failed to load resource: the server responded with a status of 403 (Forbidden)`
// ~/.hapi/settings.json config `corsOrigins`

你可以检查到目前hapi里面有多少 claude-internal 进程在跑吗?

帮我把hapi merge最新的 upstream:main



# `cc_hapi` v2
## 2026-03-25: fix `hapi` CLI & supervisor
1. hapi 如何安装到全局?
2. 是不是一定要运行一个 `hapi` 之后, 前端才能识别到该machine? 从而创建新session?
```sh
# 是的。 流程是这样的：
  hapi (CLI)                              Hub (:3006)
    │                                        │
    ├─ 启动 → Socket.IO 连接 /cli ─────────►│
    │                                        │
    ├─ 周期性发 'machine-alive' ────────────►│── handleMachineAlive()
    │   { machineId, time }                  │     → machine.active = true
    │                                        │     → broadcast 'machine-updated' → 前端
    │                                        │
    │         45秒无心跳 ───────────────────►│── expireInactive()
    │                                        │     → machine.active = false
```

- 帮我将通过npm安装的hapi删一下, 避免混淆; 配置本项目为全局 `hapi` CLI 
```sh
  ┌──────────────────────────────────────────────┬───────────────┐
  │                     操作                     │     状态      │
  ├──────────────────────────────────────────────┼───────────────┤
  │ 卸载 @tencent/hapi npm 全局包                │ ✅            │
  ├──────────────────────────────────────────────┼───────────────┤
  │ 删除 /opt/homebrew/bin/hapi-internal         │ ✅            │
  ├──────────────────────────────────────────────┼───────────────┤
  │ 创建 bin/hapi wrapper 脚本（在项目内）       │ ✅            │
  ├──────────────────────────────────────────────┼───────────────┤
  │ /opt/homebrew/bin/hapi → 软链到项目 bin/hapi │ ✅            │
  ├──────────────────────────────────────────────┼───────────────┤
  │ Force push rebased eason → origin/eason      │ ✅ up to date │
  └──────────────────────────────────────────────┴───────────────┘
```
修复merge后前端问题: 我选择 claude-internal, 但实际使用的是 claude CLI -- 你分析代码/log看一下

会不会hub是旧的没有重启? 参考 supervisor 配置, 更新一下 @/Users/frankshi/LProjects/ea-cc/supervisord.conf


## 2026-03-25: config `FAQ`
帮我梳理一下 hapi 的一些问题
- 我在另一台机器上配置的话, 如何启动/配置 hapi? 在 hapi.easonsi.site 上可以同时选择不同机器吗?
- 目前前端上只能看到通过 hapi 启动的session, 能否通过读取 ~/.claude-internal 里面的session来继续? (P2 不强求)
```sh
~/.hapi/settings.json << 'EOF'
  {
    "cliApiToken": "xxx",
    "apiUrl": "https://hapi.easonsi.site"
  }
```

帮我完全重写一下 @spec/cc_hapi/ spec 文档, 包括代码架构, FAQ, 自定义修改等
```sh
  ┌──────────────────────────┬────────────────────────────────────────────────────────────────────┐
  │           章节           │                                内容                                │   
  ├──────────────────────────┼────────────────────────────────────────────────────────────────────┤ 
  │ Architecture             │ 系统全景图、数据流、包结构、CLI 目录结构                           │   
  ├──────────────────────────┼────────────────────────────────────────────────────────────────────┤   
  │ Custom Modifications     │ 6 个自定义 commit 的详细改动表（flavor 支持 + Web UI + workdir     │   
  │                          │ 修复）                                                             │   
  ├──────────────────────────┼────────────────────────────────────────────────────────────────────┤   
  │ claude-internal vs       │ 关键差异对比表、--print 规避、session discovery 模式图解           │   
  │ claude                   │                                                                    │   
  ├──────────────────────────┼────────────────────────────────────────────────────────────────────┤   
  │ Infrastructure           │ 本地配置（supervisor/SSH tunnel）、tx01 配置、关键配置文件示例     │
  ├──────────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ Multi-Machine Setup      │ 多机架构图、新机器配置步骤、必需配置表                             │
  ├──────────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ FAQ / Troubleshooting    │ 10 个常见问题及修复方案                                            │
  ├──────────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ Operations               │ 重启、同步 upstream、状态检查的命令速查                            │
  └──────────────────────────┴────────────────────────────────────────────────────────────────────┘ 
```

## 2026-03-26: `tencent-hapi`
我在 ~/.ea/repos/_tx/tencent-hapi clone 了另一个司内版本的hapi, 
1. 帮我看一下它相较于原本 hapi 的差异主要是什么?
2. 为了不混淆, 我希望把它CLI定为 `hapi-internal`, 配置文件放到 ~/.hapi-internal/ 可否简单通过配置来实现 -- 希望在体验司内版的同时, 实现完全隔离!
```sh
# tencent-hapi vs 原版 hapi 差异
  tencent-hapi 的 origin/tencent 分支有 52 个 内部 commit（main 与上游完全一致），主要增加了：
  ┌───────────────────┬───────────────────────────────────────────────────────────────────────────┐
  │       类别        │                                   改动                                    │
  ├───────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 多用户 Token 注册 │ tokenStore.ts + admin.ts + register.ts — 管理员可创建/撤销 namespace      │
  │                   │ 隔离的 token                                                              │
  ├───────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ ASR 语音识别      │ WebSocket 反向代理 → tangredtea.devcloud.woa.com:3002，Web 端实时语音输入 │
  ├───────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 内部 Agent 命令   │ codexInternal.ts、geminiInternal.ts、codebuddy.ts — 包装司内的            │
  │                   │ Codex/Gemini/CodeBuddy                                                    │
  ├───────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ directory-scan    │ 并发安全问题、stub 文件误锁导致消息丢失                                   │
  │ 修复              │                                                                           │
  ├───────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 性能优化          │ 消息保留从 500 → 250/session，watchdog 指数退避                           │
  ├───────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 发布体系          │ @tencent/hapi v0.16.69，独立的 Hub npm 包 @tencent/hapi-hub               │
  └───────────────────┴───────────────────────────────────────────────────────────────────────────┘
# 隔离配置 ✅ 已完成
  ┌─────────────┬───────────────────────────────────┬─────────────────────────────────────────────┐
  │    维度     │            hapi (fork)            │           hapi-internal (tencent)           │
  ├─────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
  │ CLI 命令    │ /opt/homebrew/bin/hapi            │ /opt/homebrew/bin/hapi-internal             │
  ├─────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
  │ 源码        │ ~/LProjects/ea-fork/packages/hapi │ ~/.ea/repos/_tx/tencent-hapi (tencent 分支) │
  ├─────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
  │ 版本        │ 0.16.4                            │ 0.16.69                                     │
  ├─────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
  │ 数据目录    │ ~/.hapi/                          │ ~/.hapi-internal/                           │
  ├─────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
  │ Hub 端口    │ 3006                              │ 3007                                        │
  ├─────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
  │ cliApiToken │ 独立                              │ 独立（已自动生成新 token）                  │
  ├─────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
  │ machineId   │ 独立                              │ 独立                                        │
  ├─────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
  │ SQLite DB   │ ~/.hapi/hapi.db                   │ ~/.hapi-internal/hapi.db                    │
  └─────────────┴───────────────────────────────────┴─────────────────────────────────────────────┘
```

sync spec, add a new file to record tencent-hapi & update/add ref in the main spec file



# Notes
## [notes]
- 相关配置
    - spec @spec/cc_hapi/spec.md 文件
    - supervisor @/Users/frankshi/LProjects/ea-cc/supervisord.conf | easons_agent 中统一管理supervisor
    - `cliApiToken`: 它同时用于 Web 认证和 CLI ↔ Hub 通信
- 架构: 
- QA
    - 多machine启动? OK, 参见文档, 需要配置相同 `cliApiToken`
- features: (梳理需求)
- `hapi-internal` repo/CLI? CLI  & config/data 完全隔离
    - ~/.ea/repos/_tx/tencent-hapi [git.woa](https://git.woa.com/ViteOps/tencent-hapi/)
    - 配置统一放到 ~/.hapi-internal/


## [todo]
- core features
    - [x] 部署 relay/web 页面: -- `https://hapi.easonsi.site`
    - [x] sep `hapi-internal` and `hapi` -- 采用不同 HAPI_HOME & HAPI_LISTEN_PORT
- hapi hub
    - [ ] skills support
    - [ ] files explorer: ln -s
    - [ ] hooks -- 还是会弹权限提醒 (实际上CC已经自动通过运行了)
    - [ ] model -- 目前setting参数无法传入
- [ ] dev v.s. compile?
- [ ] 本地历史的 cc 执行记录
- misc
    - [ ] spec sync

## [spec]
- 实现思路
    - `hapi`（裸命令）启动时的自动行为: 1) Auto-start Hub (在之前运行过的情况下); 2) Auto-start Runner; 3) 启动 Claude 会话（runClaude()）— 连接 Hub，开始交互
    - CC 接入方案: 区分 remote/local 模式 [deepwiki](https://deepwiki.com/search/-hapi-claudecode-remotelocal_8c675ef5-2729-4216-ace8-bd7a22027f8f?mode=fast)
- 修改点:
    - 增加 claude-internal 的启动逻辑
    - ssh tunnel 实现 [web](https://hapi.easonsi.site) & [local](http://localhost:3006/) 访问 hapi hub
- 安全
    - hapi.easonsi.site 安全性: 加密形式就是 **token-based auth**, 和 app.hapi.run 的安全模型一致

resources
- `hapi` [github](https://github.com/tiann/hapi); [docs](https://hapi.run/docs/guide/quick-start.html)
- 内网版 [tencent-hapi](https://git.woa.com/ViteOps/tencent-hapi/); [km](https://km.woa.com/articles/show/653720) 文档
